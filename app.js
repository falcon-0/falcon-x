const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const cart=[];const drawer=$('#drawer'),scrim=$('#scrim'),toast=$('#toast');
function money(n){return new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n)}function renderCart(){const box=$('#cartItems');$('#bagCount').textContent=cart.length;$('#cartTotal').textContent=money(cart.reduce((n,x)=>n+x.price,0));box.innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-row"><span>${x.name}</span><b>${money(x.price)}</b><button data-remove="${i}">×</button></div>`).join(''):'<p class="empty">Your bag is waiting for impact.</p>';$$('[data-remove]').forEach(b=>b.onclick=()=>{cart.splice(+b.dataset.remove,1);renderCart()})}function openBag(){drawer.classList.add('open');scrim.classList.add('on');drawer.setAttribute('aria-hidden','false')}function closeBag(){drawer.classList.remove('open');scrim.classList.remove('on');drawer.setAttribute('aria-hidden','true')}function ping(msg){toast.textContent=msg;toast.classList.add('on');setTimeout(()=>toast.classList.remove('on'),1800)}
$$('[data-add]').forEach(b=>b.onclick=()=>{cart.push({name:b.dataset.add,price:+b.dataset.price});renderCart();ping(`${b.dataset.add.toUpperCase()} ADDED`)});$('#bagBtn').onclick=openBag;$('#closeBag').onclick=closeBag;scrim.onclick=closeBag;$('#checkoutBtn').onclick=()=>{if(!cart.length)return ping('YOUR BAG IS EMPTY');closeBag();$('#checkout').showModal()};$('#closeCheckout').onclick=()=>$('#checkout').close();$('#checkoutForm').onsubmit=e=>{e.preventDefault();e.currentTarget.style.display='none';$('#success').style.display='block';cart.length=0;renderCart()};$('#newsletter').onsubmit=e=>{e.preventDefault();e.currentTarget.reset();ping('SIGNAL LOCKED — WELCOME, PILOT')};$$('.notify').forEach(b=>b.onclick=()=>ping('BLACKWING SIGNAL TRACKED'));
const launch=Date.now()+8.5*24*36e5;setInterval(()=>{let s=Math.max(0,Math.floor((launch-Date.now())/1000)),d=Math.floor(s/86400);s%=86400;let h=Math.floor(s/3600);s%=3600;let m=Math.floor(s/60);$('#countdown').textContent=[d,h,m,s%60].map(x=>String(x).padStart(2,'0')).join(':')},1000);let sound=false;$('#soundBtn').onclick=()=>{sound=!sound;$('#soundBtn').textContent=`◉ SOUND ${sound?'ON':'OFF'}`;ping(sound?'AMBIENCE ENABLED':'AMBIENCE MUTED')};document.addEventListener('mousemove',e=>{document.documentElement.style.setProperty('--mx',e.clientX+'px');document.documentElement.style.setProperty('--my',e.clientY+'px')});renderCart();
